# DeepSeek 文案引擎
DEEPSEEK_API_KEY=
LLM_MODEL=deepseek-chat
# Resend 邮件（不填则只生成不发信）
RESEND_API_KEY=
MAIL_TO=
# Cron 鉴权（Vercel Cron 自动带 Bearer $CRON_SECRET）
CRON_SECRET=
# Vercel Blob 读写令牌（连接 Blob store 后自动注入）
BLOB_READ_WRITE_TOKEN=
# 站点地址（邮件里的网页版链接）
SITE_URL=https://daily.saveme505.help
